// stateless component
import React from "react";

const Button = (props) => {
  return <button>{props.name}</button>;
};

export default Button;

// export default (props) => {
//   return <button>{props.name}</button>;
// };
